namespace Omni.AutoApi
{
    /// <summary>
    /// Marker interface que identifica serviços a serem expostos como API (lado servidor)
    /// e consumidos por proxies de cliente (lado cliente).
    /// </summary>
    public interface IRemoteService
    {
    }
}
